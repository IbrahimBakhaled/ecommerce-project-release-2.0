import { ShoppylandValidators } from './shoppyland-validators';

describe('ShoppylandValidators', () => {
  it('should create an instance', () => {
    expect(new ShoppylandValidators()).toBeTruthy();
  });
});
